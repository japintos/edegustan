# Desarrollo: API en otro puerto (npm run dev).
VITE_API_URL=http://localhost:4000/api

# Produccion con web y API en el mismo dominio (npm run build + NODE_ENV=production):
# no definas VITE_API_URL en el build; el cliente usa /api en el mismo origen.
